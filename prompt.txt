You are Claude Code working inside the Aequitas repository. The system currently has two critical regressions:

1) The default superuser account "actpm@aequitas.com" cannot log in (login returns 401).
2) The registration UI is broken because the frontend expects an auth config object (allow_public_signup) which is undefined; also the Pay & Register flow is failing or falling back inconsistently.

Your task: implement a robust, idempotent patch that fixes authentication, the auth/config endpoint, the sign-up & payment flows, and adds defensive logging and fallbacks so the frontend never crashes reading undefined.

Requirements (high level)
- Fix backend login/auth so that existing seeded superuser can authenticate using the password from DEFAULT_SUPERUSER_PASSWORD in .env (idempotent seed).
- Ensure an endpoint `/api/v1/auth/config` (or whatever frontend expects) reliably returns JSON with `allow_public_signup` and other flags; backend must always return valid JSON (with defaults) even on error.
- Make frontend registration code defensive: check for undefined before accessing `allow_public_signup`, and use a sensible default (false).
- Fix paid registration flow: if Stripe keys are missing, return a clear JSON response indicating `stripe_configured: false` and enable a mock mode only when `ALLOW_MOCK_PAYMENTS=true` env var exists.
- Add temporary server-side debug logs for auth, config fetch, and stripe-related actions; keep logs minimal and non-sensitive (do not print passwords).
- Add tests for: seed creation, config endpoint, free registration allowed vs forbidden, and paid registration fallback.

Concrete Backend tasks (FastAPI + SQLAlchemy)
1. Startup seed:
   - In `app/core/config.py`: add DEFAULT_SUPERUSER_EMAIL, DEFAULT_SUPERUSER_PASSWORD, ALLOW_PUBLIC_SIGNUP, ALLOW_MOCK_PAYMENTS env vars (with defaults).
   - In startup script (e.g., `app/db/init_db.py` or `app/main.py` on startup event) ensure:
     * If DEFAULT_SUPERUSER_EMAIL and DEFAULT_SUPERUSER_PASSWORD are set and user not exists, create superuser (is_superuser=True) using existing password hashing util.
     * Log: "Default superuser created: actpm@aequitas.com" (no password).
   - Ensure this is idempotent (do not update existing user).

2. Login verification:
   - Ensure login endpoint (`/api/v1/auth/login-json`) verifies password using current hasher. If the codebase changed the hasher, add a legacy-check fallback: if verify_password(old_algo) passes, re-hash using new hasher and persist.
   - Add clear error messages in logs on failed authentication (e.g., "Failed login for email X: user not found" or "password mismatch").

3. Auth config endpoint:
   - Add or fix `GET /api/v1/auth/config` returning:
     {
       "allow_public_signup": <bool from config.ALLOW_PUBLIC_SIGNUP>,
       "stripe_configured": <true|false>,
       "mock_payments_allowed": <true|false>
     }
   - Always return 200 with JSON. If an internal exception occurs, return defaults with a `warning` field.

4. Registration endpoints:
   - Validate `register_type` and enforce server-side guard:
     * If register_type == "free": require ALLOW_PUBLIC_SIGNUP true else return 403 with helpful message.
     * If register_type == "paid": if STRIPE_API_KEY present create Stripe session, else if ALLOW_MOCK_PAYMENTS true create a mock paid pending registration. If neither, return 503 with message "payments unavailable".
   - Ensure pending registration record created for paid flows with token & expiry.
   - Implement webhook endpoint that finalizes pending registration upon confirmed payment.
   - Ensure `create_user()` is only called in flows where company association is present or special-case superuser seed.

5. Add/adjust unit tests:
   - test_superuser_seeding()
   - test_config_endpoint_returns_defaults()
   - test_free_registration_guard()
   - test_paid_registration_fallback_to_mock()

Concrete Frontend tasks (React + TS)
1. Defensive config fetch:
   - Update `frontend/src/pages/auth/Register.tsx` (or wherever fetchConfig is called) to:
     * Try/catch the fetch of `/api/v1/auth/config`.
     * If response missing or malformed, fallback to `{ allow_public_signup: false, stripe_configured: false, mock_payments_allowed: false }`.
     * Do not access `allow_public_signup` without checking presence.
   - Add a small spinner and a user-friendly message if payments are unavailable.

2. Login diagnostics:
   - On login failure show server message if present (e.g., "Invalid credentials" vs "account locked").
   - Ensure login request uses `email` and `password` fields (not uid).

3. Payment UI:
   - If `stripe_configured` false and `mock_payments_allowed` false, hide "Pay & Register" button and show explanation: "Payments not configured. Contact admin."
   - If `stripe_configured` false and `mock_payments_allowed` true, make the Pay & Register button trigger a mock checkout flow calling the backend confirm endpoint.

4. Minimal tests:
   - verify that Register page does not crash when `/auth/config` returns 500 or empty.
   - verify login displays server error on 401.

Deliverables
- File-by-file diffs in ```diff``` blocks for all changed files.
- Short explanation for each change (1-2 sentences).
- Tests added and high-level results (pass/fail).
- Add a short README note describing env variables and the mock payments fallback.

Safety & non-breaking constraints
- Do not log secrets.
- Do not overwrite an existing superuser.
- Keep behavior backwards-compatible for other modules.

Begin now. Provide diffs and test changes when finished.
